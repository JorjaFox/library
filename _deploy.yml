method: rsync
site_dir: _site

user: jorjafox@ftp.jorjafox.net
remote_path: /home/jorjafox/public_html/library
delete: true
port:                                     # If your host requires a non standard port
flags:  -avzt                             # Modify flags as necessary to suit your hosting setup

# exclude:                                # files to exclude
# exclude-from:                           # Path to file containing list of files to exclude
# include:                                # files to include
# include-from:                           # Path to file containing list of files to include